<block wx:for="{{list}}">
    <t-cell title="{{item.text}}" hover description="{{item.date}}">
        <t-switch bind:change="changeSwitch" value="{{item.isUserBan}}" slot="note" data-id="{{item.id}}" data-uid="{{item.uid}}"/>
    </t-cell>
</block>